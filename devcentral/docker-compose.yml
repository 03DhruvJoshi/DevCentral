version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    container_name: devcentral-db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - devcentral_pg_data:/var/lib/postgresql/data

volumes:
  devcentral_pg_data:
